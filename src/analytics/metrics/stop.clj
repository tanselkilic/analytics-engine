(ns analytics.metrics.stop
  (:gen-class))

(defn metrics-ops [data]
  (println "Got a stop metric")

  ;; duration per channel

  ;; duration per site

  ;; duration per app

  ;; duration per user

  ;; duration per session

  ;; duration per campaign

  ;; duration per device

  ;; duration per locale

  ;; duration per location

  ;; duration per network

  ;; duration per os

  ;; duration per referrer

  ;; duration per screen

  ;; duration per timezone

  ;; duration per property


  )
